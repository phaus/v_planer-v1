<% [:street, :postalcode, :locality, :country, :phone, :fax, :mobile].each do |name| %>
  <div class="control-group">
    <%= f.label name, :class => 'control-label'  %>
    <div class="controls">
      <%= f.text_field name, :class => 'input-xlarge' %>
    </div>
  </div>
<% end %>