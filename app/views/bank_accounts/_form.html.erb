<%= hidden_field_tag :come_from, 'bank_details'  %>
<% [:bank_name, :registrar_name, :number, :blz, :iban, :bic].each do |name| %>
  <div class="control-group">
    <%= f.label name, :class => 'control-label'  %>
    <div class="controls">
      <%= f.text_field name, :class => 'input-xlarge' %>
    </div>
  </div>
<% end %>